{
  "name": "matrix-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace web run dev",
    "build": "npm --workspace web run build",
    "start": "npm --workspace web run start",
    "lint": "npm --workspace web run lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
