"use client";

// The "clinic" is the signed-in, key-unlocked workspace that owns patients and
// messages. Workspace-level logic (membership, settings, directory) will live
// here. Placeholder export keeps this a valid module + entry point.
export {};
